meta {
  name: Join Family
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/families/{{familyId}}/join
}

headers {
  Content-Type: application/json
}

body:json {
  "userId": "{{memberUserId}}",
  "displayName": "Avery Parker",
  "addedByUserId": "{{addedByUserId}}"
}
