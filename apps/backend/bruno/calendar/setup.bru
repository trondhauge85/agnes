meta {
  name: Setup
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/calendar/setup
}

headers {
  Content-Type: application/json
}

body:json {
  "provider": "{{calendarProvider}}",
  "authorizationCode": "{{authorizationCode}}",
  "redirectUri": "{{redirectUri}}"
}
