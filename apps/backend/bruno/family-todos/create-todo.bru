meta {
  name: Create Family Todo
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/families/{{familyId}}/todos
}

headers {
  Content-Type: application/json
}

body:json {
  "title": "Order groceries",
  "notes": "Add snacks for game night.",
  "status": "open",
  "assignedToUserId": "{{memberUserId}}"
}
